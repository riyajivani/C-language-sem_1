main()
{
    float temp,cels;
        printf("Enter the value of temperature in celcius:\n");
        scanf("%f",&cels);
    temp=((9*cels)/5)+32;
        printf("the value of temperature ic fahrenheit is %f",temp);
    getch();
}
